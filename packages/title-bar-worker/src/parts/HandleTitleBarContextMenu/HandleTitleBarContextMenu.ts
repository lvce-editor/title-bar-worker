import type { TitleBarMenuBarState } from '../TitleBarMenuBarState/TitleBarMenuBarState.ts'

export const handleTitleBarContextMenu = async (state: TitleBarMenuBarState): Promise<TitleBarMenuBarState> => {
  return state
}
