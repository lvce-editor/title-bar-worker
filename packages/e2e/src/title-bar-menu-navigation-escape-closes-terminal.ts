import { createEscapeClosesMenuTest } from './shared/menuItem.ts'

export const name = 'title-bar-menu-navigation-escape-closes-terminal'
export const test = createEscapeClosesMenuTest(6, 'Terminal')
