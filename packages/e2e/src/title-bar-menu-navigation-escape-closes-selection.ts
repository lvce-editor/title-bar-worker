import { createEscapeClosesMenuTest } from './shared/menuItem.ts'

export const name = 'title-bar-menu-navigation-escape-closes-selection'
export const test = createEscapeClosesMenuTest(2, 'Selection')
